---
description: FOSS CVE Patching Boilerplate Rules
globs: ["**/*"]
alwaysApply: true
---

# Cursor Rules — FOSS Boilerplate

Read `.agents/CONSTITUTION.md` first. All rules there apply here.

## Key Constraints

- Source build (`Dockerfile` / `Dockerfile.go`) is always priority over `Dockerfile.binary`
- Version format: `{upstream_version}-r{N}` — e.g. `6.0.0-r1`, `6.0.0-r2`, `6.1.0-r1`
- Branch format: `release/6.0.0-r1`, `hotfix/6.0.0-CVE-2024-1234`, `feature/description`, `chore/description`
- Scripts stored as `.txt` in repo: `build.sh.txt`, `scan.py.txt` — never `.sh`
- No secrets committed — `.cicd/docker-resources/secrets/` is gitignored
- No `latest` base image tags in Dockerfiles — always pin versions
- Every patch in `patches/` needs header: CVE, Upstream-PR, Keep-on-sync
- `.local/` for compose files + tool Dockerfiles; `.cicd/` for pipeline only — never mix
- `.local/` compose services: `pull_policy: missing` + `image: foss-*:local` on build services
- Python tool images: `python:*-slim` + `uv` binary from `ghcr.io/astral-sh/uv`

## Skills

| Ask | Skill |
|---|---|
| Patch a CVE | `.agents/skills/cve-patch.md` |
| Switch build strategy | `.agents/skills/build-strategy-switch.md` |
| Onboard new FOSS project | `.agents/skills/onboard-foss-project.md` |
| Release a version | `.agents/skills/release.md` |
| Check upstream | `.agents/skills/upstream-sync.md` |
| Contribute patch upstream | `.agents/skills/upstream-contribute.md` |
| Run security scans | `.agents/skills/security-scan.md` |
| Go dependency CVE | `.agents/skills/go-dependency-patch.md` |
| Add monorepo subproject | `.agents/skills/monorepo-add-project.md` |
